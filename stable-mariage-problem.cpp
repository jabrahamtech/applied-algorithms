#include <iostream>
#include <map>
#include <string>

using namespace std;


int main(){
    string n;
    cin >> n;
    n.erase(0,3);
    int n = stoi(n);
    int total = stoi(n) * 2;
    

    
    map<int, int> person;
    for (int i=1; i == total; i++){
        cin >> 
    }
    return 0;
}